import 'package:flutter/material.dart';

class AppColors {
  static const cream = Color(0xFFFAF3E1);   // backgrounds
  static const beige = Color(0xFFF5E7C6);   // secondary surfaces / cards
  static const accent = Color(0xFFFA8112);  // buttons, highlights
  static const darkText = Color(0xFF222222); // text
}
